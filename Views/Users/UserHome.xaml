<Window x:Class="WPFK.Views.UserHome"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Moje przesyłki" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Twoje przesyłki" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <ListView x:Name="ParcelListView" Height="400">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="30"/>
                        <GridViewColumn Header="Nadawca" DisplayMemberBinding="{Binding Sender}" Width="100"/>
                        <GridViewColumn Header="Odbiorca" DisplayMemberBinding="{Binding Recipient}" Width="100"/>
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="100"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <Button Content="Odśwież" Margin="0,10,0,0"  VerticalAlignment="Bottom"/>
        </Grid>
    </Grid>
</Window>
